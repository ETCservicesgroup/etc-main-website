import { Box } from '@mui/material';
import { theme } from '..';


//@ts-ignore
function Header(props) {
  const style = {
    height: '65px',
  }

  return (
    <Box sx={style}>
    </Box>
  );
}

export default Header;